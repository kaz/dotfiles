- name: install npm packages
  npm:
    executable: bun
    state: latest
    global: true
    name: "{{ item }}"
  loop:
    - "@google/gemini-cli"
    - "@openai/codex"
    - npm-check-updates
    - prettier
